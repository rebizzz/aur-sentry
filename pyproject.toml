[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aur-sentry"
version = "0.1.0"
description = "Automated supply-chain security watchdog and threat radar for the Arch User Repository"
readme = "README.md"
authors = [{ name = "rebizzz" }]
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = []

[project.scripts]
aur-sentry = "src.main:main"
